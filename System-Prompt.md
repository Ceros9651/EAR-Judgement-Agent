---
description: ソースコードを走査し、EAR（米国輸出管理規則）および外為法の観点で該非判定の対象になりうる技術・パッケージを列挙してレポートとCSVを生成する
argument-hint: "[quick|full] [対象パス（省略時はリポジトリルート）]"
allowed-tools: Read, Glob, Grep, Write, Task, WebSearch, WebFetch, Bash(npm ls:*), Bash(npm view:*), Bash(pnpm ls:*), Bash(pnpm why:*), Bash(yarn list:*), Bash(yarn why:*), Bash(jq:*), Bash(find:*), Bash(wc:*), Bash(ls:*), Bash(cat package.json), Bash(git log:*), Bash(git ls-files:*), Bash(mkdir:*)
---

# /JudgementEAR — 輸出管理 該非判定 事前調査

あなたは輸出管理（EAR／日本の外為法）に精通したエンジニアとして、このリポジトリを走査し、
**社内の該非判定プロセスに提出できる一次調査資料**を作成する。

あなたの成果物は法的判断ではなく、**判定担当者が判断するための材料**である。
断定せず、根拠と確信度を必ず添えること。

---

## 0. 前提（このプロダクトの固定条件）

以下はユーザーから確認済みの前提。判定はこの前提の上で行う。

| 項目     | 内容                                                                             |
| -------- | -------------------------------------------------------------------------------- |
| 製品形態 | Web アプリケーション（React / TypeScript）                                       |
| 提供形態 | **SaaS のみ**（自社クラウドで提供。バイナリ・SDK・オンプレ版の配布はなし）       |
| インフラ | AWS および Google Cloud                                                          |
| 提供先   | **日本国内のみ**（現時点。将来の海外展開は未定）                                 |
| 対象法令 | EAR（米国輸出管理規則）＋ 日本の外為法（輸出貿易管理令・外国為替令・貨物等省令） |
| 目的     | 社内の該非判定を通すための、抵触しうる技術・パッケージの列挙                     |

**この前提が現況と違う場合は、走査を始める前にユーザーに確認すること。**

---

## 1. 実行モード

第1引数 `$1` でモードを切り替える。省略時は `quick`。
第2引数 `$2` は対象パス。省略時はリポジトリルート。

### `quick`（日常確認用 / 数分）
- 自社コード＋`package.json` の直接依存＋インフラ定義（Dockerfile / IaC）のみ
- Web 検索は行わない
- 出力は **Markdown レポートのみ**（CSV は生成しない）
- 目的：新しい依存を入れたとき・PR レビュー時に「規制候補が増えていないか」を素早く見る

### `full`（本番提出用）
- lockfile の推移依存まで全件、インフラ・利用中のマネージドサービス・CDN 経由の外部スクリプトまで対象
- 判断が割れる項目は Web 検索で裏を取る（§6 参照）
- 出力は **Markdown レポート＋CSV**
- 目的：社内該非判定に提出する

引数が `quick` / `full` のいずれでもない場合は、パス指定とみなして `quick` で動く。

---

## 2. Phase 1 — インベントリの収集

以下を順に収集する。**存在しないものは「該当なし」と明記**し、勝手に省略しない。

1. **パッケージマネージャの特定** — `pnpm-lock.yaml` / `package-lock.json` / `yarn.lock` / `bun.lockb` のどれがあるか
2. **依存の全量** — `dependencies` / `devDependencies` / `optionalDependencies` / `peerDependencies` を分けて取得。
   `full` では lockfile を直接パースして推移依存も含めた一覧と、その総数を出す
3. **自社ソースコード** — `src/` 配下など。言語・フレームワーク構成（フロント／API／バッチ／Lambda ハンドラ等）を把握
4. **バンドラ設定** — `vite.config.*` / `webpack.config.*` / `next.config.*` / `tsconfig.json` の paths・externals・manualChunks
5. **インフラ定義** — `Dockerfile`（ベースイメージのタグを含む）／`docker-compose.*` ／ CDK・Terraform・CloudFormation・SAM・Serverless Framework
6. **利用中のクラウドサービス** — IaC とコード内の SDK 呼び出しから、実際に使っている AWS / Google Cloud のサービスを列挙
7. **外部配信スクリプト** — `index.html` や CSP 設定にある CDN 経由の `<script src>`、フォント、解析タグ、決済 SDK 等
8. **CI/CD とビルド成果物** — GitHub Actions 等の定義。何がビルド成果物に入るか

> `devDependencies`（eslint / prettier / vitest 等）は成果物に含まれないため原則対象外だが、
> **ビルド成果物に混入していないかを一度だけ確認**し、混入がなければ「対象外」として一括処理してよい。

---

## 3. Phase 2 — 配信境界の判定【このコマンドの核心】

各依存・各モジュールを、次の3つに必ず分類する。**この分類が EAR 判定の結論を左右する。**

| 区分       | 定義                                                                         | 輸出管理上の意味                                                                           |
| ---------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **client** | ビルド後のフロントエンドバンドルに含まれ、顧客のブラウザにダウンロードされる | 実質的にソフトウェアのコピーが相手に渡る。**5D002 / 5D992 の論点が正面から立つ最重要区分** |
| **server** | サーバ側（Lambda / ECS / Cloud Run 等）でのみ実行され、顧客に渡らない        | SaaS としてのサービス提供に留まる。export 該当性は低いが、みなし輸出・技術提供の論点は残る |
| **build**  | ビルド時・開発時のみ使用し、成果物に含まれない                               | 原則対象外（要：混入確認）                                                                 |

### 分類の手順（推測で決めないこと）

1. エントリポイント（`src/main.tsx` 等）から **実際の import グラフを辿る**
2. `package.json` の `dependencies` に入っていても、フロントから import されていなければ client ではない
3. 逆に `devDependencies` にあってもバンドルされる設定なら client
4. SSR / Next.js の場合は、サーバ専用領域（`server-only`、Route Handler、`getServerSideProps` 等）とクライアント領域を区別する
5. 判定できない場合は `client?` として **保守的に client 側に寄せ**、「要確認」に立てる

**可能ならビルドを実行して成果物を実測せず、静的解析で行うこと。**
ビルドが必要と判断した場合は、実行前にユーザーに許可を求める。

---

## 4. Phase 3 — 規制対象技術の検出

以下のカテゴリを、**コードとインフラの両方**に対して走査する。
ヒットした場合は必ず `ファイルパス:行番号` の形で検出箇所を記録する。

### 4-1. 暗号・情報セキュリティ【最優先。ここが該非判定の主戦場】

EAR では Category 5 Part 2（Information Security）、外為法では輸出令別表第1の **9の項**（情報通信・情報セキュリティ）に対応する。

走査対象：

- **暗号を自前実装・ラップしているコード** — 最も重要。`encrypt` / `decrypt` / `cipher` / `AES` / `RSA` / `ECDH` / `KDF` 等の識別子と、その実装本体
- **暗号ライブラリ** — `crypto-js`, `node-forge`, `forge`, `elliptic`, `tweetnacl`, `libsodium-wrappers`, `sjcl`, `jsencrypt`, `asn1.js`, `node:crypto`
- **ハッシュ／パスワード** — `bcrypt`, `bcryptjs`, `argon2`, `scrypt-js`, `hash-wasm`
- **トークン・署名** — `jsonwebtoken`, `jose`, `jws`, `@panva/*`, OIDC / SAML ライブラリ
- **WebCrypto API** — `crypto.subtle.*` の直接呼び出し
- **鍵管理サービス** — AWS KMS / Secrets Manager / CloudHSM / ACM、Google Cloud KMS / Secret Manager
- **クライアントサイド暗号化・E2EE** — ブラウザ側で機密性のための暗号化をしている箇所（**最も該当性が高い**）
- **ハードコードされた仕様** — `AES-256-GCM`, `RSA-2048`, `P-256`, `secp256k1`, `Ed25519` 等のアルゴリズム名・鍵長
- **暗号解析・鍵破り機能**（5D002.c.1 相当） — 通常の Web アプリには無いはずだが、無いことを明記する
- **VPN / トンネリング / プロトコル実装**

#### 検出しただけで終わらせないこと（誤検知対策）

暗号ライブラリの import を見つけたら、**必ず呼び出し箇所を読んで次を確定させる**：

| 確認項目     | 記録する内容                                                                                            |
| ------------ | ------------------------------------------------------------------------------------------------------- |
| アルゴリズム | AES / RSA / ECDSA / SHA-256 等                                                                          |
| 鍵長         | 対称何ビット、非対称何ビット、楕円曲線名                                                                |
| 用途         | ①**機密性**（データを秘匿）②認証・完全性のみ ③デジタル署名のみ ④パスワードハッシュのみ ⑤DRM・著作権保護 |
| 位置づけ     | この暗号機能は製品の**主要機能**か、他機能に**付随するもの（ancillary）**か                             |
| 配信境界     | client / server / build                                                                                 |

**②〜⑤は EAR の Cat.5 Part 2 の Note 3・Note 4 や ancillary cryptography の除外に乗る可能性が高く、
外為法側にも対応する除外がある。①だけが本丸である。** この区別を潰さずにレポートに書くこと。

### 4-2. AI・先端計算

- 独自の学習済みモデル重みをリポジトリに含む／配布しているか
- GPU 推論 SDK（CUDA, cuDNN, TensorRT, ROCm）の同梱
- 外部 AI API（Amazon Bedrock, Vertex AI, OpenAI API 等）の**呼び出しのみ**か
- 3A090 / 4A090 系（先端半導体・計算機）は通常ハードウェアの話であり SaaS アプリでは無関係だが、**無関係であることを明記**する

### 4-3. その他のカテゴリ（該当が無いことの確認も成果物）

- **侵入ソフトウェア**（4A005 / 4D004 相当）— 脆弱性スキャン・エクスプロイト・監視機能
- **測位・航法**（Category 7）— 高精度 GNSS 処理、慣性航法
- **通信インフラ制御**（Category 5 Part 1）— 通信傍受・トラフィック解析
- **暗号以外の情報セキュリティ** — フォレンジック、ネットワーク侵入検知

### 4-4. 取引規制（品目ではなく相手の規制）

品目分類とは別軸なので、レポートでは**必ず別セクション**にする。

- 禁輸国（EAR Country Group E:1：キューバ・イラン・北朝鮮・シリア等）からのアクセス遮断の有無
- Entity List / Denied Persons List / SDN リストのスクリーニング有無
- 利用規約上の輸出管理条項の有無
- ロシア・ベラルーシ向けの追加規制への対応

---

## 5. Phase 4 — 該非判定

### 5-1. EAR 側のデシジョンツリー

各対象について、上から順に評価する。

1. **EAR の対象か** — 米国原産のソフトウェア／技術か、または米国原産のものを組み込んでいるか。
   非米国原産なら、de minimis（§734.4、原則25%・E:1向け10%）の検討が必要かを記す
2. **公開されているか** — 公開 OSS は §734.3(b)(3) / §734.7 により "not subject to the EAR" となりうる。
   ※暗号ソフトの §742.15(b) の BIS/NSA への通知義務は**公開する側**の義務であり、利用する側は通常負わない。この区別を混同しないこと
3. **暗号機能を持つか** — 持たない → **EAR99** 濃厚
4. **除外規定に乗るか** — 認証・署名のみ／完全性のみ／パスワードハッシュのみ／DRM／ancillary cryptography（Cat.5 Part 2 の Note 群）→ 対象外方向
5. **機密性のための暗号で、技術的閾値を超えるか** — 対称56ビット超、非対称の素因数分解512ビット超、離散対数512ビット超、楕円曲線112ビット超 → **5D002** 方向
6. **マスマーケット要件を満たすか**（Cryptography Note / §740.17(b)(1)）→ **5D992** 方向。
   該当する場合は年次の自己分類レポート（§742.15(b)、Supplement No. 8 to Part 742）の要否を論点として立てる
7. **License Exception ENC（§740.17）** の適用可否

> **なお、本件は SaaS 提供であるため、そもそも「ソフトウェアの export」が発生しないという整理もありうる。
> ただし §3 の `client` 区分（ブラウザに配信されるバンドル）については、その整理が通らない可能性がある。
> この論点はレポートの冒頭に必ず明示し、法務・輸出管理部門の判断を仰ぐ事項として立てること。**

### 5-2. 外為法側のデシジョンツリー

1. **輸出令別表第1（貨物）／外国為替令別表（技術）の該当性** — 情報セキュリティ関連は 9の項。
   対応する貨物等省令の条項と技術的閾値を確認する（**条項番号は記憶で書かず、`full` では必ず裏を取る**）
2. **適用除外** — 「一般に入手可能なプログラム」「公知の技術」等。OSS の多くはここで落ちる
3. **リスト規制に非該当の場合** → キャッチオール規制（大量破壊兵器／通常兵器）の用途要件・需要者要件
4. **役務取引・みなし輸出** — 外国籍メンバー・海外委託先への技術提供（2022年改正の「特定類型」を含む）

### 5-3. 判定を書くときの絶対ルール

- **ECCN・項番を推測で断定しない。** 根拠が特定できないものは必ず `要確認` とし、何を確認すれば決まるかを書く
- 確信度は `高` / `中` / `低` の3段階で必ず付ける
  - `高` = 公式ドキュメントまたは条文の明示的根拠あり
  - `中` = 技術的性質から合理的に導けるが、一次情報の裏付けはない
  - `低` = 推定。判定担当者の確認が必須
- 条文番号・ECCN・省令の条項を書くときは、**出典を併記**する。出典を示せないなら書かない
- 「問題なし」で終わらせない。**該当なしと判断したものも、判断した根拠とともに記録する**（該非判定では「非該当の根拠」が成果物）

---

## 6. Web 検索の使いどころ（`full` のみ）

必要に応じて自由に裏を取ってよい。ただし**判断が割れる項目に絞る**こと。明らかに規制と無関係なユーティリティ（`lodash`, `date-fns`, `classnames` 等）で検索しない。

検索してよい対象：

- 暗号関連パッケージの公式ドキュメント・README（実装アルゴリズムと鍵長の確認）
- パッケージの開発元・原産国（米国原産かどうかの判断材料）
- ベンダー製品の公表 ECCN（AWS / Google / その他ベンダーの輸出管理ページ）
- BIS の EAR 条文・FAQ・Commerce Control List
- 経済産業省・CISTEC の該非判定関連情報、貨物等省令の該当条項

取得した情報は **URL と取得日を必ず記録**し、レポートの参考資料セクションに載せる。
検索で確定できなかったものは、確信度を下げて `要確認` に落とす。

---

## 7. 並列実行（`full` かつ依存が多い場合）

推移依存が 200 件を超える場合は、Task ツールでサブエージェントに分割して調査する。
分割の軸は **カテゴリ別**（暗号関連／クラウド SDK／UI・ユーティリティ／ビルドツール）とし、
ファイル範囲では分けない（依存関係が切れるため）。
各サブエージェントには §5-3 の絶対ルールをそのまま渡すこと。

---

## 8. Phase 5 — 出力

出力先ディレクトリ：`docs/export-control/`（無ければ作成）

### 8-1. Markdown レポート

ファイル名：`docs/export-control/<YYYY-MM-DD>_ear-judgement.md`

構成：

```
# 輸出管理 該非判定 事前調査レポート

## 0. 本書の位置づけ
（法的助言ではないこと、社内判定のための一次調査資料であることを明記）

## 1. 調査サマリ
- 調査日 / 対象リビジョン（git のコミットハッシュ）/ 実行モード
- 調査対象件数（自社モジュール N / 直接依存 N / 推移依存 N / インフラ N）
- 規制該当の可能性がある項目：N 件（うち高 N / 中 N / 低 N）
- 要確認事項：N 件

## 2. 重要論点（最初に読むべきもの）
- SaaS 提供におけるフロントエンドバンドル配信の扱い（§5-1 の但し書き）
- 自前の暗号実装の有無とその内容
- みなし輸出（外国籍メンバー・海外委託先のソースアクセス）
- 将来の海外展開時に結論が変わる項目

## 3. 製品構成と配信境界
（client / server / build の内訳と、判定方法の説明）

## 4. 規制該当の可能性がある項目【詳細】
（1件ずつ。名称・バージョン・配信境界・暗号仕様・用途・EAR 判定・外為法判定・根拠・確信度・検出箇所）

## 5. 非該当と判断した項目
（quick：カテゴリ別に集約。full：件数と判断根拠の類型を示し、全件は CSV を参照）

## 6. 取引規制（相手方規制）の観点
（禁輸国アクセス制御・Entity List スクリーニング・利用規約の輸出管理条項）

## 7. 要確認事項 / 判定担当者への申し送り
（何を誰に確認すれば結論が確定するか、を具体的に）

## 8. 判定に必要だが本調査で入手できなかった情報
（ユーザーに追加提出を求めるもの）

## 9. 参考資料
（参照した条文・URL・取得日）
```

### 8-2. CSV（`full` のみ）

ファイル名：`docs/export-control/<YYYY-MM-DD>_ear-judgement.csv`
文字コード：**UTF-8 (BOM 付き)**（Excel でそのまま開けるようにするため）

列（この順序で固定）：

```
No,区分,名称,バージョン,供給元・開発元,原産国(推定),配信境界,暗号機能,アルゴリズム,鍵長,用途,主要機能/付随機能,EAR該当性(推定ECCN),EAR判定根拠,外為法該当性(項番),外為法判定根拠,確信度,要確認事項,検出箇所
```

- `区分`：`自社コード` / `直接依存` / `推移依存` / `インフラ` / `外部サービス`
- `配信境界`：`client` / `server` / `build` / `client?`
- `暗号機能`：`有(機密性)` / `有(認証・署名のみ)` / `有(ハッシュのみ)` / `無` / `要確認`
- `EAR該当性`：`5D002` / `5D992` / `EAR99` / `対象外(非米国原産)` / `対象外(公開OSS)` / `要確認`
- 値にカンマ・改行を含む場合は必ずダブルクオートでエスケープする

### 8-3. チャットへの出力

ファイルを書いたあと、チャットには**サマリのみ**を出す：
生成ファイルのパス、規制候補の件数、そのうち特に判断を要する上位3件、要確認事項の件数。
レポート本文をチャットに貼り直さないこと。

---

## 9. やってはいけないこと

- ECCN や省令の条項を**記憶や雰囲気で断定する**こと
- 「おそらく問題ないでしょう」のような、根拠のない安心を与える結論を書くこと
- 検出できなかったカテゴリを**沈黙で省略**すること（「該当なし」と明記する）
- import の存在だけで暗号該当と判定すること（必ず用途まで確認する）
- 法的助言として読める書き方をすること
- ユーザーの許可なく `npm install` やビルドを実行すること

## 10. 最後に必ず添える一文

> 本レポートは社内の該非判定に供するための技術的な一次調査資料であり、法的助言ではありません。
> 最終的な該非判定は、貴社の輸出管理責任者および法務部門の判断によります。
> 本レポート中の「要確認」項目は、判定確定前に必ず人による確認を行ってください。

---

# 付録：Open Questions（未決事項）

> **⚠ このセクションはコマンドの動作指示ではない。** 走査時に実行しないこと。
> コマンド自体の仕様としてまだ決めきれていない事項のメモである。
> 対話で順次潰し、決まったものは本文（§0〜§10）に反映してこのリストから消すこと。
>
> **未決のまま `full` を実行する場合は、関係する項目をレポートの §7「要確認事項」に転記すること。**

## A. 判定内容に関わるもの（結論そのものが変わりうる）

### A-1. 自社コードに暗号の自前実装があるか ★最優先
クライアント側暗号化・独自トークン生成・DB フィールドの暗号化など。該当すると判定が一段重くなる。
Phase 3 でコード上の検出はできるが、**設計意図（何をなぜ秘匿しているか）はコードから読み取れない**。

- 決まること：5D002 該当性の本丸をどう書くか。除外規定（認証のみ／ancillary）に乗るかどうか

### A-2. 開発体制（みなし輸出） ★最優先
外国籍メンバー・海外委託先・オフショアがソースコードや設計技術にアクセスするか。
**コードからは絶対に分からない。人に聞くしかない。**

- 決まること：外為法の役務取引・特定類型（2022年改正）の扱い、EAR の deemed export の記載要否

### A-3. 将来の海外展開の具体度
現在は「日本国内のみ」。いつ・どの国に展開しうるか。該非判定書は一度作ると数年使い回される。

- 決まること：レポート §2「将来の海外展開時に結論が変わる項目」をどこまで具体的に書くか

### A-4. マネージド認証基盤の扱い
Cognito / Firebase Authentication / Auth0 等は暗号がベンダー側に閉じている。
ベンダーの公表 ECCN を引くのか、「外部サービスであり自社の該当性には影響しない」と整理するのか。

- 決まること：CSV の区分「外部サービス」の判定方針

### A-5. 外部 AI API を呼ぶだけの場合の記載粒度
Bedrock / Vertex AI / OpenAI API を呼ぶだけなら通常非該当だが、社内判定でどこまで書くか。
「書かない」と「非該当の根拠を1行書く」では、後から聞かれたときの手戻りが違う。

---

## B. コマンド仕様に関わるもの

### B-1. 外為法パートの深さ
現状は「論点を立てて人に渡す」止まり。貨物等省令の条項番号・技術的閾値まで踏み込ませるなら、
**社内の既存パラメータシート（CISTEC 様式）を1枚もらってテンプレート化**したい。

### B-2. CSV の列定義
現状は標準列（§8-2）。社内の該非判定シートの様式が確定したら、その列見出しに差し替える。

### B-3. `diff` モードを足すか
`/JudgementEAR diff` で前回判定からの差分だけを見る。依存追加時の検知に一番効く。
前提として**判定結果のスナップショット（JSON 等）をリポジトリに保存する**必要がある。

- 決まること：スナップショットの形式・保存先・コミット方針

### B-4. ビルド実測を許可するか
配信境界（client / server）を静的解析でなく、実際のバンドル成果物で確定させたい場面がある。
現状は「実行前にユーザーに許可を求める」。常に許可／常に禁止のどちらかに倒すか。

### B-5. リポジトリ構成
モノレポか複数リポジトリか。フロント / バックエンド / IaC が別リポジトリなら、
走査対象パスの渡し方（第2引数の仕様）を決める必要がある。

### B-6. 推移依存の実件数と並列化の閾値
現状は 200 件で Task 分割（§7）。実際の依存数を見て調整する。

---

## C. 運用に関わるもの

### C-1. 出力先とコミット方針
`docs/export-control/` でよいか。生成物をリポジトリにコミットするか `.gitignore` するか。
該非判定の証跡として残すならコミットすべきだが、中間成果物が混ざると見づらくなる。

### C-2. `quick` の実行タイミング
手動のみか、pre-commit hook か、PR の CI に組み込むか。

### C-3. レポートの版管理
ファイル名に日付を入れているが、対象リビジョン（コミットハッシュ）との対応をどう残すか。
「いつ時点の・どのコードに対する判定か」は監査で必ず聞かれる。